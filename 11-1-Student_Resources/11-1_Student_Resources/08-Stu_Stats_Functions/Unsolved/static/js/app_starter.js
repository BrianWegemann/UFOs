var movieScore = [4.4, 3.3, 5.9, 8.8, 1.2, 5.2, 7.4, 7.5, 7.2, 9.7, 4.2, 6.9];

function mean(user_list){
    for (var i = 0; 1 < user_list.length; i++) {
        sum += user_list[i];
        let mean = sum / user_list.length
        console.log('The mean is ${mean}.')}};
